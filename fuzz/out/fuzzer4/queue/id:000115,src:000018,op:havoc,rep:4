int function1(in* 2;nc> g;ruct a,
				int,
	onst struct d -> int> g;

`nt ma> float> hel	hello"g3, cons c arg1 -> int> int anotherH g;
		float a = g1.v = arg2 umber;
		return arg1.value*b*2;

	hello = g @ function1;

	p(lam a -> int> {return a + 1;} @