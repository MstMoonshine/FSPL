int compare(int x, int y)
{
  if (x > y) {return 1;}
  else if (y > x) struct Person tom;
  struct Person people[10];
  tnt Š = 0;
  while (i < 10)
  {
    people[i].name = i;
    tom.friends[i] = i;
    i = i + 1;
  }
  return tom.name[i-1];
}
