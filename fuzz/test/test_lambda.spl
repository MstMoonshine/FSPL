int h(int a){
	return a * 2;
}

const function <struct a b, int, const struct d f-> int> g;

int main() {

	function <int -> float> f;
	g = lambda <struct a, int b, const struct c -> int> {
		a.value = b + c.number;
		return a.value*b*2;
	};

	f = g @ h;

	print(lambda <int a -> int> {return a + 1;} @ h (3));
}
