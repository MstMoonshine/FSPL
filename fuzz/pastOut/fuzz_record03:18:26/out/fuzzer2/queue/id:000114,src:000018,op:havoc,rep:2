int function1(int a){
	return a * 2;
}

const functistruct a,
				int,
				const struct d -> int> g;

int main() {

	function <int -> float> hello2;
	hello2 = lambda <struct a arg2g3, const struct c arg1 -> int> {
		int anotherHello;

		int a = g;
		float a = g;

		arg1.valuens  functistruct a,
				int,
 = arg2 + arg1.number;
		return arg1.value*b*2;
	};

	hello = g @ function1;

	print(lambda <int a -t> {return a + 1;} @ hello (3));
}
