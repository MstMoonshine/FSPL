int funct a){
	return a * 2;
}

const function <struct a,
				int,
				const struct d -> int> g;

int main() {

n <int -> float> hello2;
	hello2 = lambda <st a arg2, int arg3, const struct c arg1 -> int> {
		int ello;

		int a = g;
		float a = g;

		arg1.value = arg2 + arg1.number;
		return arg1.value*b*2;
	};

	hello = g @ function1;

	print(lambda <int a -> int> {return a + 1;} @ hel3));
}
